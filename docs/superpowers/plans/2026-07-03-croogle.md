# Croogle Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Задеплоить поисковик Croogle, который форвардит запросы в настоящий Google и рендерит выдачу в одной из 4 тем, где цена растёт по мере ухудшения дизайна (Шедевр $0 → Каракули $499).

**Architecture:** Next.js (App Router) на Vercel. Серверный API-роут `/api/search` прячет ключ Google Custom Search и отдаёт нормализованный JSON с fallback-кэшем. Фронт хранит `tier` в React-контексте и применяет класс темы на корне; те же компоненты результатов, 4 набора CSS.

**Tech Stack:** Next.js 14+ (App Router, TypeScript), Google Custom Search JSON API, CSS-переменные для тем, `roughjs` (опционально) для каракулей, Vercel для деплоя.

**Хакатон-режим:** «тест» на каждом шаге = живая проверка (браузер/curl), не юнит-тест. Деплоим РАНО (Task 1) и передеплоиваем часто, чтобы снять риск −10 за localhost.

---

### Task 0: Получить ключи Google Custom Search (параллельно, до кода)

**Владелец:** член команды с Google-аккаунтом. Делать в самом начале — это внешняя зависимость.

- [ ] **Step 1: Создать Programmable Search Engine**

Открыть https://programmablesearchengine.google.com/ → Add → в «Search the entire web» включить ON → создать. Скопировать **Search engine ID** (`cx`).

- [ ] **Step 2: Получить API-ключ**

Открыть https://developers.google.com/custom-search/v1/overview → «Get a Key» → создать проект → скопировать **API key**.

- [ ] **Step 3: Проверить, что ключ работает**

В браузере открыть (подставив ключи):
`https://www.googleapis.com/customsearch/v1?key=API_KEY&cx=CX&q=nfactorial`
Expected: JSON с массивом `items`, где есть `title`, `link`, `snippet`.

Записать оба значения — понадобятся в Task 1 как env-переменные.

---

### Task 1: Скаффолд Next.js + деплой пустышки на Vercel

**Files:**
- Create: весь проект через `create-next-app`

- [ ] **Step 1: Создать приложение**

Run:
```bash
npx create-next-app@latest croogle --typescript --app --eslint --no-src-dir --no-tailwind --import-alias "@/*"
```
(На вопросы отвечать дефолтами; Tailwind не нужен — темы на чистом CSS.)

Переместить содержимое в корень репо или работать внутри `croogle/` (согласовать с командой; дальше пути даны относительно корня Next-приложения).

- [ ] **Step 2: Запустить локально**

Run: `npm run dev`
Expected: открывается http://localhost:3000 со стартовой страницей Next.

- [ ] **Step 3: Задеплоить пустышку на Vercel СРАЗУ**

Run:
```bash
npx vercel --yes
```
Залогиниться, принять дефолты. Expected: получить живой `*.vercel.app` URL, страница открывается с телефона. **Это снимает риск штрафа −10.**

- [ ] **Step 4: Коммит**

```bash
git add -A
git commit -m "feat: scaffold Next.js app and deploy skeleton to Vercel"
```

---

### Task 2: API-роут поиска с fallback-кэшем

**Files:**
- Create: `app/api/search/route.ts`
- Create: `lib/fallback.ts` (захардкоженные результаты на случай лимита)
- Create: `.env.local` (НЕ коммитить — уже в .gitignore)

- [ ] **Step 1: Прописать ключи в окружение**

Создать `.env.local`:
```
GOOGLE_API_KEY=вставить_из_Task_0
GOOGLE_CX=вставить_из_Task_0
```

- [ ] **Step 2: Захардкодить fallback-кэш**

Create `lib/fallback.ts`:
```ts
export type SearchResult = { title: string; link: string; snippet: string };

// Показывается, когда Google вернул ошибку или кончился дневной лимит (100/день).
// Демо НЕ должно падать на сцене.
export const FALLBACK_RESULTS: SearchResult[] = [
  { title: "nFactorial Incubator", link: "https://nfactorial.school", snippet: "Ведущий технологический инкубатор Казахстана. Резиденты, менторы, хакатоны." },
  { title: "Worst Startup Ever — хакатон", link: "https://example.com", snippet: "Идеально реализуй абсолютно бесполезное. Uber для холодильников и другие единороги." },
  { title: "Programmable Search — Google", link: "https://developers.google.com/custom-search", snippet: "Custom Search JSON API возвращает реальную выдачу Google в формате JSON." },
  { title: "Что такое каракули как искусство", link: "https://example.com/scribbles", snippet: "Рукотворные каракули — premium-эстетика для тех, кто перерос красоту." },
];
```

- [ ] **Step 3: Написать роут**

Create `app/api/search/route.ts`:
```ts
import { NextRequest, NextResponse } from "next/server";
import { FALLBACK_RESULTS, SearchResult } from "@/lib/fallback";

export async function GET(req: NextRequest) {
  const q = req.nextUrl.searchParams.get("q")?.trim();
  if (!q) {
    return NextResponse.json({ results: [], fallback: false, empty: true });
  }

  const key = process.env.GOOGLE_API_KEY;
  const cx = process.env.GOOGLE_CX;
  const url = `https://www.googleapis.com/customsearch/v1?key=${key}&cx=${cx}&q=${encodeURIComponent(q)}`;

  try {
    const r = await fetch(url);
    if (!r.ok) throw new Error(`Google ${r.status}`);
    const data = await r.json();
    const results: SearchResult[] = (data.items ?? []).slice(0, 8).map((it: any) => ({
      title: it.title,
      link: it.link,
      snippet: it.snippet ?? "",
    }));
    if (results.length === 0) throw new Error("no items");
    return NextResponse.json({ results, fallback: false });
  } catch {
    return NextResponse.json({ results: FALLBACK_RESULTS, fallback: true });
  }
}
```

- [ ] **Step 4: Проверить роут**

Run: `npm run dev`, затем в другом терминале:
```bash
curl "http://localhost:3000/api/search?q=nfactorial"
```
Expected: JSON `{ "results": [ { "title": ..., "link": ..., "snippet": ... }, ... ], "fallback": false }`.
Проверить fallback: временно испортить `GOOGLE_API_KEY` в `.env.local`, перезапустить, повторить curl → должно прийти `"fallback": true` с FALLBACK_RESULTS.

- [ ] **Step 5: Добавить env-переменные в Vercel**

Run:
```bash
npx vercel env add GOOGLE_API_KEY production
npx vercel env add GOOGLE_CX production
```
(Вставить значения по запросу.) Так ключ будет доступен серверному роуту в проде и НЕ попадёт во фронт.

- [ ] **Step 6: Коммит**

```bash
git add app/api lib/fallback.ts
git commit -m "feat: search API route with Google Custom Search + fallback cache"
```

---

### Task 3: Состояние тарифа (ThemeContext) + определения тем

**Files:**
- Create: `lib/tiers.ts`
- Create: `app/providers/ThemeProvider.tsx`
- Modify: `app/layout.tsx`

- [ ] **Step 1: Описать тарифы**

Create `lib/tiers.ts`:
```ts
export type TierId = "masterpiece" | "design" | "sketch" | "scribble";

export type Tier = {
  id: TierId;
  name: string;
  price: number;      // $/мес
  tagline: string;
};

// Порядок = по возрастанию цены и по УБЫВАНИЮ качества (инверсия ценности).
export const TIERS: Tier[] = [
  { id: "masterpiece", name: "Шедевр",  price: 0,   tagline: "Вылизанный поиск. Для тех, кто ещё ценит красоту." },
  { id: "design",      name: "Дизайн",  price: 9,   tagline: "Чистый современный интерфейс." },
  { id: "sketch",      name: "Набросок",price: 49,  tagline: "Минимализм на грани лени." },
  { id: "scribble",    name: "Каракули",price: 499, tagline: "Рукотворные каракули под каждый запрос. Настоящая роскошь." },
];

export const DEFAULT_TIER: TierId = "masterpiece";
```

- [ ] **Step 2: Контекст темы**

Create `app/providers/ThemeProvider.tsx`:
```tsx
"use client";
import { createContext, useContext, useState, ReactNode } from "react";
import { TierId, DEFAULT_TIER } from "@/lib/tiers";

type Ctx = { tier: TierId; setTier: (t: TierId) => void };
const ThemeCtx = createContext<Ctx>({ tier: DEFAULT_TIER, setTier: () => {} });

export function ThemeProvider({ children }: { children: ReactNode }) {
  const [tier, setTier] = useState<TierId>(DEFAULT_TIER);
  return (
    <ThemeCtx.Provider value={{ tier, setTier }}>
      <div className={`theme-${tier}`}>{children}</div>
    </ThemeCtx.Provider>
  );
}

export const useTier = () => useContext(ThemeCtx);
```

- [ ] **Step 3: Обернуть приложение**

Modify `app/layout.tsx` — импортировать ThemeProvider и обернуть `{children}`; подключить глобальный CSS (Task 4):
```tsx
import "./themes.css";
import { ThemeProvider } from "@/app/providers/ThemeProvider";
// ...внутри <body>:
//   <ThemeProvider>{children}</ThemeProvider>
```

- [ ] **Step 4: Проверить**

Run: `npm run dev` → страница открывается без ошибок консоли, на корне есть класс `theme-masterpiece` (проверить в DevTools).

- [ ] **Step 5: Коммит**

```bash
git add lib/tiers.ts app/providers app/layout.tsx
git commit -m "feat: tier state via ThemeProvider context"
```

---

### Task 4: Четыре темы (CSS)

**Files:**
- Create: `app/themes.css`

- [ ] **Step 1: Написать переменные и стили тем**

Create `app/themes.css`. Каждая тема переопределяет CSS-переменные; компоненты используют только переменные, поэтому разметку менять не нужно.
```css
@import url('https://fonts.googleapis.com/css2?family=Shantell+Sans:wght@400;600&family=Inter:wght@400;600&display=swap');

:root {
  --bg: #fff; --fg: #202124; --accent: #1a0dab; --card-bd: #dfe1e5;
  --radius: 12px; --font: 'Inter', system-ui, sans-serif; --skew: 0deg;
  --shadow: 0 1px 6px rgba(32,33,36,.28); --card-bg: #fff;
}

/* Шедевр ($0) — вылизанный тёмный премиум */
.theme-masterpiece {
  --bg: #0e0e10; --fg: #f1f1f3; --accent: #8ab4f8; --card-bg: #1b1b1f;
  --card-bd: #2a2a30; --radius: 16px; --shadow: 0 8px 30px rgba(0,0,0,.5);
  --font: 'Inter', sans-serif;
}
.theme-masterpiece .result { transition: transform .2s, box-shadow .2s; }
.theme-masterpiece .result:hover { transform: translateY(-2px); box-shadow: 0 12px 40px rgba(0,0,0,.6); }

/* Дизайн ($9) — чистый светлый */
.theme-design { --bg:#fff; --fg:#202124; --accent:#1a73e8; --radius:12px; --shadow:0 1px 6px rgba(32,33,36,.18); }

/* Набросок ($49) — плоский студенческий, без теней и радиусов */
.theme-sketch { --bg:#f4f4f4; --fg:#111; --accent:#0044cc; --radius:0px; --shadow:none; --card-bd:#999; }

/* Каракули ($499) — рукописный шрифт, кривые рамки, лёгкий наклон */
.theme-scribble {
  --bg:#fffdf5; --fg:#1a1a1a; --accent:#c0392b; --font:'Shantell Sans', cursive;
  --radius:255px 15px 225px 15px/15px 225px 15px 255px; /* «нарисованная от руки» рамка */
  --shadow:none; --card-bd:#333; --skew:-0.6deg;
}
.theme-scribble .result { transform: rotate(-0.4deg); border: 2px solid var(--card-bd); }
.theme-scribble .result:nth-child(even) { transform: rotate(0.5deg); }

/* Общие стили через переменные */
body { background: var(--bg); color: var(--fg); font-family: var(--font); }
.result {
  background: var(--card-bg); border: 1px solid var(--card-bd);
  border-radius: var(--radius); box-shadow: var(--shadow);
  padding: 16px; margin: 12px 0; transform: skewY(var(--skew));
}
.result a { color: var(--accent); text-decoration: none; font-size: 18px; }
.result a:hover { text-decoration: underline; }
.result .url { color: #0a7d33; font-size: 13px; }
.result .snippet { margin-top: 4px; font-size: 14px; opacity: .9; }
```

- [ ] **Step 2: Проверить визуально (после Task 5/6 будет что смотреть)**

Пока просто убедиться, что `npm run dev` не падает и шрифты грузятся (вкладка Network → Shantell Sans 200 OK).

- [ ] **Step 3: Коммит**

```bash
git add app/themes.css
git commit -m "feat: four tier themes (masterpiece -> scribble) via CSS vars"
```

---

### Task 5: Страница поиска (SearchBar + ResultsList)

**Files:**
- Create: `app/search/page.tsx`
- Create: `components/SearchBar.tsx`
- Create: `components/ResultsList.tsx`

- [ ] **Step 1: ResultsList**

Create `components/ResultsList.tsx`:
```tsx
"use client";
import { SearchResult } from "@/lib/fallback";

export function ResultsList({ results }: { results: SearchResult[] }) {
  return (
    <div>
      {results.map((r, i) => (
        <div className="result" key={i}>
          <a href={r.link} target="_blank" rel="noreferrer">{r.title}</a>
          <div className="url">{r.link}</div>
          <div className="snippet" dangerouslySetInnerHTML={{ __html: r.snippet }} />
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: SearchBar**

Create `components/SearchBar.tsx`:
```tsx
"use client";
import { useState } from "react";

export function SearchBar({ onSearch }: { onSearch: (q: string) => void }) {
  const [q, setQ] = useState("");
  return (
    <form
      onSubmit={(e) => { e.preventDefault(); onSearch(q); }}
      style={{ display: "flex", gap: 8, maxWidth: 640, margin: "24px auto" }}
    >
      <input
        className="search-input" value={q} onChange={(e) => setQ(e.target.value)}
        placeholder="Что вы хотели найти? (мы тоже забыли)"
        style={{ flex: 1, padding: 12, borderRadius: "var(--radius)", border: "1px solid var(--card-bd)", background: "var(--card-bg)", color: "var(--fg)", fontFamily: "var(--font)" }}
      />
      <button type="submit" style={{ padding: "12px 20px", borderRadius: "var(--radius)", border: "none", background: "var(--accent)", color: "#fff", cursor: "pointer", fontFamily: "var(--font)" }}>
        Найти
      </button>
    </form>
  );
}
```

- [ ] **Step 3: Страница поиска**

Create `app/search/page.tsx`:
```tsx
"use client";
import { useState } from "react";
import { SearchBar } from "@/components/SearchBar";
import { ResultsList } from "@/components/ResultsList";
import { SearchResult } from "@/lib/fallback";

export default function SearchPage() {
  const [results, setResults] = useState<SearchResult[]>([]);
  const [loading, setLoading] = useState(false);
  const [searched, setSearched] = useState(false);

  async function onSearch(q: string) {
    if (!q.trim()) return;
    setLoading(true); setSearched(true);
    const r = await fetch(`/api/search?q=${encodeURIComponent(q)}`);
    const data = await r.json();
    setResults(data.results ?? []);
    setLoading(false);
  }

  return (
    <main style={{ minHeight: "100vh", padding: 24 }}>
      <h1 style={{ textAlign: "center", fontFamily: "var(--font)" }}>Croogle</h1>
      <SearchBar onSearch={onSearch} />
      {loading && <p style={{ textAlign: "center" }}>Рисуем результаты вручную…</p>}
      {!loading && searched && results.length === 0 && (
        <p style={{ textAlign: "center" }}>Вы, кажется, забыли, что искали. Как и все.</p>
      )}
      <div style={{ maxWidth: 680, margin: "0 auto" }}>
        <ResultsList results={results} />
      </div>
    </main>
  );
}
```

- [ ] **Step 4: Проверить**

Run: `npm run dev` → открыть /search → ввести «nfactorial» → появляются реальные результаты Google в текущей теме. Проверить пустой запрос и загрузку.

- [ ] **Step 5: Коммит**

```bash
git add app/search components/SearchBar.tsx components/ResultsList.tsx
git commit -m "feat: search page with live Google results"
```

---

### Task 6: Fake Paywall + переключатель тарифа (демо-магия)

**Files:**
- Create: `components/TierSwitcher.tsx`
- Create: `components/PaywallModal.tsx`
- Modify: `app/search/page.tsx` (вставить переключатель)

- [ ] **Step 1: PaywallModal — псевдо-чекаут, который просто меняет тему**

Create `components/PaywallModal.tsx`:
```tsx
"use client";
import { TIERS, TierId } from "@/lib/tiers";
import { useTier } from "@/app/providers/ThemeProvider";

export function PaywallModal({ open, onClose }: { open: boolean; onClose: () => void }) {
  const { setTier } = useTier();
  if (!open) return null;
  function choose(id: TierId) { setTier(id); onClose(); }
  return (
    <div onClick={onClose} style={{ position: "fixed", inset: 0, background: "rgba(0,0,0,.5)", display: "grid", placeItems: "center", zIndex: 50 }}>
      <div onClick={(e) => e.stopPropagation()} style={{ background: "var(--card-bg,#fff)", color: "var(--fg,#111)", padding: 24, borderRadius: 16, maxWidth: 720, width: "90%" }}>
        <h2>Выберите ваш уровень эстетики</h2>
        <p style={{ opacity: .7 }}>Чем выше тариф — тем эксклюзивнее (и хуже) выглядит поиск. Оплата ненастоящая — это демо.</p>
        <div style={{ display: "grid", gridTemplateColumns: "repeat(auto-fit,minmax(150px,1fr))", gap: 12 }}>
          {TIERS.map((t) => (
            <button key={t.id} onClick={() => choose(t.id)}
              style={{ padding: 16, borderRadius: 12, border: "1px solid #ccc", cursor: "pointer", textAlign: "left", background: "#fafafa" }}>
              <strong>{t.name}</strong>
              <div style={{ fontSize: 22 }}>${t.price}<span style={{ fontSize: 12 }}>/мес</span></div>
              <div style={{ fontSize: 12, opacity: .7 }}>{t.tagline}</div>
              <div style={{ marginTop: 8, fontSize: 12, color: "#1a73e8" }}>Оформить →</div>
            </button>
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: TierSwitcher — быстрый переключатель для демо на сцене**

Create `components/TierSwitcher.tsx`:
```tsx
"use client";
import { useState } from "react";
import { TIERS } from "@/lib/tiers";
import { useTier } from "@/app/providers/ThemeProvider";
import { PaywallModal } from "./PaywallModal";

export function TierSwitcher() {
  const { tier, setTier } = useTier();
  const [payOpen, setPayOpen] = useState(false);
  const current = TIERS.find((t) => t.id === tier)!;
  return (
    <div style={{ display: "flex", gap: 8, alignItems: "center", justifyContent: "center", flexWrap: "wrap", margin: "8px 0" }}>
      {TIERS.map((t) => (
        <button key={t.id} onClick={() => setTier(t.id)}
          style={{ padding: "6px 12px", borderRadius: 999, cursor: "pointer",
            border: t.id === tier ? "2px solid var(--accent)" : "1px solid #bbb",
            background: t.id === tier ? "var(--accent)" : "transparent",
            color: t.id === tier ? "#fff" : "var(--fg)", fontFamily: "var(--font)" }}>
          {t.name} ${t.price}
        </button>
      ))}
      <button onClick={() => setPayOpen(true)} style={{ padding: "6px 12px", borderRadius: 999, border: "1px dashed var(--accent)", background: "transparent", color: "var(--accent)", cursor: "pointer" }}>
        Апгрейд ↑
      </button>
      <PaywallModal open={payOpen} onClose={() => setPayOpen(false)} />
      <span style={{ width: "100%", textAlign: "center", fontSize: 12, opacity: .6 }}>Текущий тариф: {current.name} — {current.tagline}</span>
    </div>
  );
}
```

- [ ] **Step 3: Вставить переключатель на страницу поиска**

Modify `app/search/page.tsx` — импортировать и добавить `<TierSwitcher />` сразу под `<h1>`:
```tsx
import { TierSwitcher } from "@/components/TierSwitcher";
// ...
// <h1>Croogle</h1>
// <TierSwitcher />
// <SearchBar ... />
```

- [ ] **Step 4: Проверить демо-магию**

Run: `npm run dev` → /search → сделать поиск → переключать тарифы: интерфейс перерисовывается вживую (Шедевр = тёмный премиум, Каракули = кривой рукописный). Открыть «Апгрейд» → модалка → выбрать тариф → тема сменилась.

- [ ] **Step 5: Коммит**

```bash
git add components/TierSwitcher.tsx components/PaywallModal.tsx app/search/page.tsx
git commit -m "feat: fake paywall + live tier switcher (demo magic)"
```

---

### Task 7: Лендинг (Hero + pricing + отзывы + СМИ + FAQ)

**Files:**
- Modify: `app/page.tsx` (заменить дефолтную главную)
- Create: `components/Landing.tsx`

- [ ] **Step 1: Собрать лендинг с копирайтом уровня Series A**

Create `components/Landing.tsx` c секциями (полный текст — не плейсхолдеры):

- **Hero:** H1 «Croogle — красота для бедных. Роскошь — каракули.» · подзаголовок «Первый поисковик, где вы платите за то, как результаты выглядят. Чем дороже — тем эксклюзивнее (и хуже).» · CTA-кнопка «Найти красиво» → `href="/search"`.
- **Фейк-метрики (3 плитки):** «12.4M запросов отрендерено вручную» · «97% retention на тарифе Каракули» · «средний Каракули-юзер платит ×500 за ÷10 пикселей».
- **Pricing** — вывести `TIERS.map(...)` карточками (цена + tagline + «Выбрать»), Каракули $499 пометить бейджем «⭐ Most Premium».
- **Отзывы (3):** «Перешёл на Каракули — теперь никто не понимает мою выдачу. Статус.» — CTO стартапа · «Наконец поиск, за уродство которого не стыдно платить.» — дизайнер · «Мы закупили Enterprise-Каракули на весь отдел.» — HR.
- **«О нас в СМИ»:** логотипы-надписи TechCrunch · Forbes · ProductHunt · «Уголёк Daily» (серым, «as seen in»).
- **FAQ (4):** «Почему дороже = хуже? — Потому что красота массова, а уродство — рукотворно.» · «Это настоящий Google? — Мы честно форвардим ваш запрос в Google.» · «Могу вернуть деньги? — Каракули невозвратны, как и время.» · «Есть бесплатный тариф? — Да, Шедевр $0. Но где в этом эксклюзивность?»
- **Футер:** ссылки на `/status`, `/careers`, `/privacy`.

Каждая секция — обычный JSX с инлайновыми стилями или классами; использовать переменные темы, чтобы лендинг тоже реагировал на тариф.

- [ ] **Step 2: Подключить на главную**

Modify `app/page.tsx`:
```tsx
import { Landing } from "@/components/Landing";
export default function Home() { return <Landing />; }
```

- [ ] **Step 3: Проверить**

Run: `npm run dev` → `/` показывает лендинг, CTA ведёт на `/search`, адаптив на узком экране не ломается (DevTools → мобильный вид).

- [ ] **Step 4: Коммит**

```bash
git add app/page.tsx components/Landing.tsx
git commit -m "feat: landing page with hero, pricing, testimonials, press, FAQ"
```

---

### Task 8: «Взрослые» буллшит-детали (status / careers / privacy)

**Files:**
- Create: `app/status/page.tsx`
- Create: `app/careers/page.tsx`
- Create: `app/privacy/page.tsx`

- [ ] **Step 1: Status page**

Create `app/status/page.tsx` — заголовок «Croogle System Status», строки с зелёными точками: «Search API — Operational», «Scribble Rendering Farm — Operational (uptime 99.99%)», «Beauty Downgrade Engine — Operational», внизу «All systems nominal. Last incident: никогда (мы не храним стыд)».

- [ ] **Step 2: Careers page**

Create `app/careers/page.tsx` — вакансия «Senior Scribble Artist» (рисует каракули вручную под каждый запрос, требование: «твёрдая рука и презрение к сеткам»), «Head of Ugliness», кнопка «Apply» (alert-заглушка).

- [ ] **Step 3: Privacy page**

Create `app/privacy/page.tsx` — «Мы не храним ваши запросы. Мы храним только ваш вкус. Ваши каракули принадлежат вам (но выглядят как наши).»

- [ ] **Step 4: Проверить**

Run: `npm run dev` → открыть `/status`, `/careers`, `/privacy` из футера — все три открываются.

- [ ] **Step 5: Коммит**

```bash
git add app/status app/careers app/privacy
git commit -m "feat: bullshit polish pages (status, careers, privacy)"
```

---

### Task 9: Финальный деплой + прогон демо

- [ ] **Step 1: Задеплоить прод**

Run: `npx vercel --prod`
Expected: живой прод-URL. Открыть с телефона.

- [ ] **Step 2: Чек-лист демо (прогнать вживую, как на сцене)**

- [ ] Лендинг открывается с телефона по ссылке
- [ ] `/search` возвращает реальные результаты Google (`fallback:false` в Network)
- [ ] Fallback работает (проверить, что при ошибке показываются FALLBACK_RESULTS — не белый экран)
- [ ] Переключение тарифов на глазах меняет вид (Шедевр ↔ Каракули)
- [ ] Модалка «Апгрейд» открывается и меняет тему
- [ ] `/status`, `/careers`, `/privacy` открываются
- [ ] Адаптив на телефоне не ломается

- [ ] **Step 3: Заготовить демо-запрос**

Выбрать заранее запрос, который красиво выглядит на всех темах (напр. «nfactorial» или «unicorn startup»). Прогнать 3 раза, чтобы не упереться в лимит на сцене (у нас есть fallback, но лучше подстраховаться).

- [ ] **Step 4: Финальный коммит**

```bash
git add -A
git commit -m "chore: final demo polish and production deploy"
```

---

## Self-Review (проверка плана против спеки)

- **Инверсия тарифов** (спека §2) → Task 3 `TIERS` порядок цена↑/качество↓, Task 4 темы, Task 6 переключатель. ✅
- **Реальный поиск через Google API + fallback** (спека §3, §5) → Task 0 ключи, Task 2 роут+кэш. ✅
- **Скрытие ключа** (спека §3) → Task 2 Step 5 (env в Vercel, серверный роут). ✅
- **4 темы / демо-магия** (спека §2, §4) → Task 4 + Task 6. ✅
- **Лендинг: hero, pricing, отзывы, СМИ, FAQ** (спека §4, §8) → Task 7. ✅
- **Буллшит-детали: status/careers/privacy, UaaS-метрики** (спека §6) → Task 7 (метрики) + Task 8. ✅
- **Деплой (анти −10)** (спека §3, §8) → Task 1 Step 3 (ранний) + Task 9 (финальный). ✅
- **«Забыли, что искали» гэг** (спека §1) → Task 5 пустой-запрос сообщение + placeholder. ✅
- **Тип-консистентность:** `SearchResult` (lib/fallback) используется в роуте, ResultsList, page; `TierId`/`TIERS` (lib/tiers) — в ThemeProvider, TierSwitcher, PaywallModal, Landing. ✅
